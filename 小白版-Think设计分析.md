# 小白版 · AI项目设计书生成器 — Think 阶段设计分析

## 项目基本信息

| 项目 | 值 |
|------|------|
| 产品名 | **起步** · AI项目设计书生成器 |
| 副标题 | 一句话，秒出设计书 |
| 子域名 | `start.zhexueyuan.com` |
| GitHub 仓库 | `dunwood/ai-coach-lite` |
| 本地目录 | `D:\AI Project\ai-coach-lite` |
| 技术栈 | 纯 HTML + JS + CSS，DeepSeek API，Cloudflare Pages |
| 部署方式 | GitHub 连接 Cloudflare Pages 自动部署（git push 即上线） |
| 进阶版 | `builder.zhexueyuan.com`（dunwood/ai-project-coach，5237行，保留不动） |

---

## 一、核心问题诊断

进阶版的痛点本质是**把专业决策压力转嫁给了用户**。一个说"我想做记账app"的小白，脑子里只有一个模糊画面，你问他"目标用户是谁？数据持久化方案？是否需要多端同步？"——他不是不想回答，是根本不知道这些问题意味着什么。

小白版的核心设计原则：**AI 替用户做决策，而不是问用户做决策。**

用户给一句话，AI 根据经验做出所有合理假设，直接输出结果。用户看到结果后觉得"哇，这就是我想要的"或者"差不多，但我想改一下这个"——这时候修改的门槛就很低了，因为他已经有一个具体的东西可以参照。

---

## 二、核心流程设计

```
用户输入一句话（或点示例）
        ↓
   点击"生成设计书"
        ↓
  AI 流式输出设计书（15-30秒）
        ↓
  用户看到完整设计书
        ↓
  ┌─────┼─────┐
  复制   下载   想要更详细？→ 进阶版
```

**关键体验：零提问。** 用户从输入到看到结果，中间没有任何对话、没有任何选择、没有任何等待页面跳转。就是一个输入框、一个按钮、一份设计书。

---

## 三、AI 生成的设计书内容结构

对小白来说，设计书不需要面面俱到，只需要让他感觉"我的想法被具体化了，我知道下一步该做什么"。

### 设计书包含以下 6 个部分：

1. **项目名称**（AI 起一个朗朗上口的名字）
2. **一句话定位**：这个产品是什么、给谁用、解决什么问题
3. **核心功能清单**（3-5 个，不多不少）：每个功能一句话描述
4. **页面规划**（2-4 个页面）：每个页面包含什么内容
5. **技术方案建议**：用什么技术做（大白话，非技术人员能看懂）
6. **执行路线图**：第一步做什么、第二步做什么（3-5 步）

**总量控制**：一屏到两屏能看完，约 500-800 字。

---

## 四、差异化体验设计

### 4.1 示例灵感墙
输入框下面放 6-8 个可点击的示例卡片，如"记账 app"、"读书笔记工具"、"健身打卡"、"宠物喂养提醒"等。小白可能连想法都说不清楚，点一个示例马上就能看到效果，降低第一步的心理门槛。

### 4.2 生成后的微调（v2 再做）
设计书生成之后，底部放 2-3 个简单开关，如"需要用户登录吗？"、"做网页版还是手机 app？"点击后重新生成。**第一版先不做，保持极简。**

### 4.3 历史记录
用 localStorage 保存之前生成的设计书，下次回来还能看到。**第一版做，代码量不大，体验提升明显。**

### 4.4 到进阶版的桥梁
"想要更专业的设计书？"按钮引导到 builder.zhexueyuan.com。**第一版先放一个简单链接，不做参数传递。**

---

## 五、技术架构

### 文件结构（极简）

```
ai-coach-lite/
├── index.html          # 唯一页面（单页应用）
├── css/
│   └── style.css       # 全部样式
├── js/
│   ├── app.js          # 主逻辑（输入处理、UI 状态控制）
│   ├── api.js          # DeepSeek API 调用 + 流式输出处理
│   ├── prompts.js      # Prompt 模板（系统提示词 + 设计书格式要求）
│   └── storage.js      # localStorage 历史记录管理
├── CLAUDE.md           # Claude Code 项目规范说明
└── .gitignore          # 忽略 .env 等敏感文件
```

### 技术要点

| 要点 | 方案 |
|------|------|
| AI 调用 | DeepSeek API，endpoint: `https://api.deepseek.com/v1/chat/completions`，model: `deepseek-chat` |
| 流式输出 | `stream: true`，用 `ReadableStream` + `TextDecoder` 逐字显示 |
| API Key | 前端 JS 内写死（和进阶版一致，确认后沿用） |
| 数据持久化 | localStorage 存历史设计书（JSON 数组） |
| 设计书渲染 | AI 返回 Markdown，前端用简单的 Markdown → HTML 转换显示 |
| 复制功能 | `navigator.clipboard.writeText()` 复制 Markdown 原文 |
| 下载功能 | 生成 .md 文件下载 |
| 响应式 | 纯 CSS media query，适配手机和桌面 |

### 不做的事（技术约束）

- ❌ 不用 React / Vue / Next.js
- ❌ 不用 webpack / vite
- ❌ 不用 tailwind / bootstrap
- ❌ 不创建 `functions/` 目录
- ❌ 不创建 `wrangler.toml` / `wrangler.jsonc` / `wrangler.json`
- ❌ 不用 Gemini API
- ❌ 不用 Vercel

### 预估代码量

800-1200 行（进阶版的 1/5 左右）。

---

## 六、Prompt 设计思路（核心）

小白版的质量完全取决于 Prompt。核心思路：

```
系统提示词要做到：
1. 角色设定：你是一个资深产品经理，擅长把模糊的想法变成清晰的产品方案
2. 输入假设：用户只会给你一句话，你需要自己做出所有合理假设
3. 输出格式：严格按照 6 个部分输出，用 Markdown 格式
4. 语言风格：大白话，不用专业术语，让完全不懂技术的人也能看懂
5. 长度控制：500-800 字，不要太长吓跑用户
6. 技术建议：默认推荐"纯网页（HTML+JS）"方案，因为最适合小白用 AI 辅助开发
```

---

## 七、页面 UI 布局草案

```
┌─────────────────────────────────────────┐
│          起步 · AI项目设计书生成器         │
│          一句话，秒出设计书               │
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────────────────────────────┐    │
│  │  描述你的想法...（输入框）        │    │
│  └─────────────────────────────────┘    │
│              [ 生成设计书 ]              │
│                                         │
│  💡 不知道做什么？试试这些：              │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐  │
│  │记账app│ │读书笔记│ │健身打卡│ │宠物提醒│  │
│  └──────┘ └──────┘ └──────┘ └──────┘  │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐  │
│  │番茄钟 │ │菜谱收藏│ │日记本 │ │背单词 │  │
│  └──────┘ └──────┘ └──────┘ └──────┘  │
│                                         │
├─────────────────────────────────────────┤
│                                         │
│  📄 设计书输出区域（流式显示）            │
│  ─────────────────────────────          │
│  （AI 生成的设计书内容在这里逐字出现）     │
│                                         │
│  [ 复制 ]  [ 下载 ]  [ 想要更详细？→ ]   │
│                                         │
├─────────────────────────────────────────┤
│                                         │
│  📋 历史记录（最近生成的设计书）           │
│  · 记账app设计书 — 2025-06-01           │
│  · 读书笔记设计书 — 2025-05-30          │
│                                         │
├─────────────────────────────────────────┤
│  哲学园出品 · 进阶版：builder.xxx.com    │
└─────────────────────────────────────────┘
```

---

## 八、待确认事项

在进入 Plan/Task 阶段之前，需要确认：

1. **产品名"起步"是否满意？** 子域名 `start.zhexueyuan.com` 是否可以？
2. **API Key 处理方式**：进阶版是用户自填 key 还是你的 key 写在前端 JS 里？小白版沿用同样方式。
3. **第一版功能范围确认**：
   - ✅ 一句话输入 → 设计书生成（核心）
   - ✅ 示例灵感墙（8个可点击示例）
   - ✅ 流式输出
   - ✅ 复制 + 下载
   - ✅ 历史记录（localStorage）
   - ✅ 到进阶版的简单链接
   - ❌ 微调开关（v2）
   - ❌ 参数传递到进阶版（v2）
4. **Markdown 渲染**：是用一个轻量库（如 marked.js，CDN 引入）还是自己写简单的正则转换？

确认后，我出完整的 task 拆分计划，然后生成 task01.md。

---

## 九、Task 拆分预览（待确认后细化）

| Task | 内容 | 预估 |
|------|------|------|
| task01 | 项目初始化 + CLAUDE.md + 基础 HTML 骨架 + 样式 | 200行 |
| task02 | DeepSeek API 调用 + 流式输出 + Prompt 模板 | 300行 |
| task03 | 示例灵感墙 + 输入交互优化 | 100行 |
| task04 | 复制/下载功能 + 历史记录 | 200行 |
| task05 | 进阶版链接 + 页脚 + 移动端适配 + 细节打磨 | 100行 |

预计 5 个 task 完成全部 v1 功能。
